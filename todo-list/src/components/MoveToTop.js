import React from "react";

const MoveToTop = (props) => {

    return (
        <>
            <button className="form-button" onClick={props.moveToTop}>Move to top</button>
        </>
    )
}

export default MoveToTop